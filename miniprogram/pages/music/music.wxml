<view class="music-page">
  <text class="title">音乐播放器</text>
  <button 
    class="play-button {{isPlaying ? 'playing' : ''}}" 
    bindtap="togglePlay"
  >
    {{isPlaying ? '暂停' : '播放'}}
  </button>
  <text class="status-text">{{isPlaying ? '正在播放中...' : '点击按钮开始播放'}}</text>
</view> 